import { FC } from "react";

const Header: FC = () => {
    return (
        <header>
            <nav className="dark:bg-gray-800 flex justify-center items-center text-white py-5">
                search field login to save favorite city
            </nav>
        </header>
    );
};
export default Header;
